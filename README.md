
## Dependencies
Install these prerequisites to follow along with the tutorial. See free video tutorial or a full explanation of each prerequisite.
- NPM: https://nodejs.org
- Hardhat: https://hardhat.org/
- EthersJs: https://ethers.org/
- Tailwind CSS: https://tailwindcss.com/

## Step 1. 
run 'npm install' (or 'yarn install' for MacOS).
## Step 2. 
run 'npm start' (or 'yarn start' for MacOS).

Visit this URL in your browser: http://localhost:3000



- timelock trong solidity 
- remove owner from contract
- BE: 
    - user:
        + register
        + login
        + list campaigns: thêm filter vào
        + get campaign by id
        + connect wallet: metamask, rút tiền, check balance, ...
        + donate to campaign
        + check campaign status: open, closed, expired, successful, tiền được bao nhiêu, ...
        + get user donation history
    - admin:
        + create campaign
    - deploy contract(tương tác với file sol)
        + smart contract 
        + set timelock 
        + rut quyển owner
- FE: 
    - tạo giao diện cho các chức năng BE ở trên (figma để gen, add api)

